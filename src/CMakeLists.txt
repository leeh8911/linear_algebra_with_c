file(GLOB_RECURSE SRC_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

add_library(LINEAR_ALGEBRA_WITH_CPP STATIC
${SRC_FILES})
target_include_directories(LINEAR_ALGEBRA_WITH_CPP PUBLIC 
${CMAKE_SOURCE_DIR}/src
)